<h2>Sections</h2>

<%= link_to "Create new sections", new_section_path(:id => @page.id) %>
<hr>
<ul>
  <% @section.sort_by{|section| section.position}.each do |section| %>
  <li><%= link_to (section.name), edit_section_path(:id => section.id) %></li>
  <% end %>
</ul>